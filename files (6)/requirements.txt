# MoodLearn — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core API ─────────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
bcrypt==4.1.2
pydantic[email]==2.7.0
motor==3.3.2
pymongo==4.6.3

# ── TensorFlow (facial model) ─────────────────────────────────────────────────
tensorflow==2.15.0          # or tensorflow-gpu==2.15.0 for GPU
# EfficientNetB2 is included in tf.keras.applications — no extra install needed

# ── PyTorch + HuggingFace (voice model) ──────────────────────────────────────
# Install PyTorch from https://pytorch.org for your CUDA version.
# CPU-only example (slow but works):
torch==2.2.2
torchaudio==2.2.2
transformers==4.40.0
datasets==2.19.0
accelerate==0.29.3

# ── Audio processing ──────────────────────────────────────────────────────────
librosa==0.10.1
soundfile==0.12.1
audioread==3.0.1

# ── Image processing ──────────────────────────────────────────────────────────
opencv-python==4.9.0.80
mediapipe==0.10.11
albumentations==1.4.4

# ── ML utilities (training scripts) ──────────────────────────────────────────
scikit-learn==1.4.2
matplotlib==3.8.4
seaborn==0.13.2
tqdm==4.66.2

# ── Gemini ────────────────────────────────────────────────────────────────────
google-genai==0.5.0

# ── Misc ─────────────────────────────────────────────────────────────────────
numpy==1.26.4
bson==0.5.10
